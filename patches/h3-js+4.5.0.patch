diff --git a/node_modules/h3-js/.DS_Store b/node_modules/h3-js/.DS_Store
new file mode 100644
index 0000000..b541f58
Binary files /dev/null and b/node_modules/h3-js/.DS_Store differ
diff --git a/node_modules/h3-js/dist/browser/h3-js.es.js b/node_modules/h3-js/dist/browser/h3-js.es.js
index a047624..4275673 100644
--- a/node_modules/h3-js/dist/browser/h3-js.es.js
+++ b/node_modules/h3-js/dist/browser/h3-js.es.js
@@ -257,7 +257,9 @@ var libh3 = function (libh3) {
   function stringToUTF8(str, outPtr, maxBytesToWrite) {
     return stringToUTF8Array(str, HEAPU8, outPtr, maxBytesToWrite);
   }
-  var UTF16Decoder = typeof TextDecoder !== "undefined" ? new TextDecoder("utf-16le") : undefined;
+  // Expo's Hermes TextDecoder currently supports UTF-8 only. Fall back to
+  // Emscripten's built-in UTF-16 conversion instead of failing at startup.
+  var UTF16Decoder = undefined;
   function writeArrayToMemory(array, buffer) {
     HEAP8.set(array, buffer);
   }
diff --git a/node_modules/h3-js/dist/browser/h3-js.js b/node_modules/h3-js/dist/browser/h3-js.js
index 024799a..c90aae5 100644
--- a/node_modules/h3-js/dist/browser/h3-js.js
+++ b/node_modules/h3-js/dist/browser/h3-js.js
@@ -257,7 +257,9 @@ var libh3 = function (libh3) {
   function stringToUTF8(str, outPtr, maxBytesToWrite) {
     return stringToUTF8Array(str, HEAPU8, outPtr, maxBytesToWrite);
   }
-  var UTF16Decoder = typeof TextDecoder !== "undefined" ? new TextDecoder("utf-16le") : undefined;
+  // Expo's Hermes TextDecoder currently supports UTF-8 only. Fall back to
+  // Emscripten's built-in UTF-16 conversion instead of failing at startup.
+  var UTF16Decoder = undefined;
   function writeArrayToMemory(array, buffer) {
     HEAP8.set(array, buffer);
   }
